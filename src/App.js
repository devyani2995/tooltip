// Import the Tooltip component
import Tooltip from "./components/Tooltip";

// Define the main App component
function App() {
  {/* Render the Tooltip component with the position prop */}
  return (
    <div>
      <Tooltip position="bottom" />
    </div>
  );
}

export default App;
